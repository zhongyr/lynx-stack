{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["tests/**", "shell-project", "build/**", "resources/**"],
      "outputs": ["dist"],
      "env": ["PORT", "CI"]
    }
  }
}
