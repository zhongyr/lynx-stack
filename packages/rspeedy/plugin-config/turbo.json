{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        "src",
        "rslib.config.ts",
        "tsconfig.build.json"
      ],
      "outputs": [
        "dist"
      ]
    }
  }
}
