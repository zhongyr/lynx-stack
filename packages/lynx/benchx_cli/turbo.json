{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["ECOSYSTEM_CI"],
      "passThroughEnv": ["ECOSYSTEM_CI"],
      "dependsOn": [],
      "inputs": ["scripts/build.sh"],
      "outputs": ["dist/**"]
    }
  }
}
