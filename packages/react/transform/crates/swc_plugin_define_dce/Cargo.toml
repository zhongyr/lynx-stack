[package]
name = "swc_plugin_define_dce"
version = "0.1.0"
edition = "2021"

[lib]
path = "lib.rs"

[dependencies]
napi = { workspace = true, optional = true }
napi-derive = { workspace = true, optional = true }
serde = { workspace = true, features = ["derive"] }

[features]
napi = ["dep:napi", "dep:napi-derive"]
